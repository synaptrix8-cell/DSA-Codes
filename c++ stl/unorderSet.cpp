#include<bits/stdc++.h>
using namespace std;


void explainUnorderSet(){

    unordered_set<int>st;

    //lower_bound and upper_bound function does not work.
    //rest all function are same as above .
    // it does not stores in any perticular order.
    //it has a complexity than set in most cases, except some when collision happen


    //it does not store in stored order, it store in randomly.
    // but it store in unique manner.
}